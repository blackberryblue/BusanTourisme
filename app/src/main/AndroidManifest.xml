<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.com.busantourisme">

    <uses-permission android:name="android.permission.INTERNET" />

    //전화 걸기
    <uses-permission android:name="android.permission.CALL_PHONE" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BusanTourisme"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.post.Tour.TourCommentActivity"
            android:parentActivityName=".view.post.Tour.TourDetailActivity"/>
        <activity android:name=".view.post.festival.FestivalActivity" />
        <activity android:name=".view.post.etcView.UserInfoActivity" />
        <activity android:name=".view.post.etcView.SearchActivity" />
        <activity android:name=".view.post.Tour.TourDetailActivity"
            android:parentActivityName=".view.post.MainActivity"/>
        <activity android:name=".view.auth.LoginActivity" />
        <activity android:name=".view.auth.JoinActivity" />
        <activity android:name=".view.bar.AppBarActivity" />
        <activity android:name=".view.post.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
